MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/neuracare?retryWrites=true&w=majority
PORT=5000
JWT_SECRET=changeme
JWT_EXPIRES_IN=1h
# Deployed frontend origin, allowed by CORS in addition to http://localhost:5173.
# e.g. https://your-app.vercel.app - not needed for local development.
FRONTEND_URL=
