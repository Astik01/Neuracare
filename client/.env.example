# Base URL of the backend API, including the /api prefix.
# Defaults to http://localhost:5000/api if unset - only needed to point at a
# deployed backend (e.g. https://your-backend.onrender.com/api).
VITE_API_URL=
